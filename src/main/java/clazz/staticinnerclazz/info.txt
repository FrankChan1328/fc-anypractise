静态内部类的好处
	1).提高封装性，从代码位置上讲，静态内部类放置在外部类内；从代码层意义上讲：静态内部类是外部类的子行为或者子属性，两者保持着一定的关系。
	2).提高了代码的可读性。
	3).形似内部，神似外部。静态内部类虽然存在于外部类内，而且编译后的文件名也包含外部类(格式：外部类+$+内部类)，但是它可以脱离外部类存在。
	也就是说，可以通过new Home() 声明一个Home 对象，只是需要导入Person.Home 而已。
	
3.静态内部类和外部类之间有着强关联关系。这不仅仅表现在"字面上"，而深层次的抽象意义则依赖于类的设计。