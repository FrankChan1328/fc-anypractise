1.关于反射需要知道的：
	1).java 语言特性之一
	2).允许程序在运行时访问 对象的属性、方法、构造方法等

2.反射的主要应用场景
	1).开发通用框架：比如Spring，可能需要根据配置文件加载不同的对象或类，调用不同的方法，需要用到反射：运行时动态加载。
	2).动态代理：比如AOP 拦截特定的方法
	3).注解：注解本身只是标记，需要利用反射机制，根据标记去调用注解解释器，执行行为。
	4).可扩展性功能：应用程序可以通过使用完全限定名称创建可扩展对象实例来使用外部的用户定义类
	
3.反射的缺点
	1).性能开销
	2).破坏封装性
	3).内部曝光